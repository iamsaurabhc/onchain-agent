{
  "name": "@onchain-agent/anchor-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared chain access, config, and types for anchoring & verification (Phase E).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./test/fakeRegistry": "./test/helpers/fakeRegistry.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@onchain-agent/hash-core": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
