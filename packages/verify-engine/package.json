{
  "name": "@onchain-agent/verify-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Unified six-method verification engine for AnchorRegistry (Phase E).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:amoy": "vitest run --config vitest.amoy.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@onchain-agent/anchor-client": "workspace:*",
    "@onchain-agent/hash-core": "workspace:*"
  },
  "devDependencies": {
    "@onchain-agent/anchor-client": "workspace:*",
    "@types/node": "^22.10.0",
    "fast-check": "^3.23.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
